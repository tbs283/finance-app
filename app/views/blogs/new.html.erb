<%= form_with model:@blog_new, url:blogs_path, local:true do |f| %>
    <div class="field">
        <%= f.label :title %><br/>
        <%= f.text_field :title %>    <%= f.submit '投稿する' %>
    </div>
    
    <div class="field">
        <%= f.label :content %><br/>
        <%= f.text_area :content, size: "150x100" %>    
    </div>
<% end %>